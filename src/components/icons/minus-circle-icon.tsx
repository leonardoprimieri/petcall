import { MinusCircle } from "phosphor-react-native";

import { IconPropsType } from "./types";

export const MinusCircleIcon = (props: IconPropsType) => {
  return <MinusCircle color="#fff" {...props} />;
};
