import { PlusCircle } from "phosphor-react-native";

import { IconPropsType } from "./types";

export const PlusCircleIcon = (props: IconPropsType) => {
  return <PlusCircle color="#fff" {...props} />;
};
