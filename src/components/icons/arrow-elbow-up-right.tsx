import { ArrowElbowUpRight } from "phosphor-react-native";

import { IconPropsType } from "./types";

export const ArrowElbowUpRightIcon = (props: IconPropsType) => {
  return <ArrowElbowUpRight {...props} />;
};
