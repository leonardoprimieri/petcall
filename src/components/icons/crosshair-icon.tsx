import { Crosshair } from "phosphor-react-native";

import { IconPropsType } from "./types";

export const CrossHairIcon = (props: IconPropsType) => {
  return <Crosshair color="#fff" {...props} />;
};
